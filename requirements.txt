# 버전을 고정한다.
#
# 모델은 joblib 피클로 저장했다가 다음 실행에서 되읽는다. 절인 쪽과 푸는 쪽의
# scikit-learn 이 다르면 사설 클래스가 사라져 그대로 깨진다. pandas 3.0 처럼
# 동작이 바뀌는 상위 버전이 조용히 끼어드는 것도 같은 이유로 막는다.
#
# 경마 버전(horseai)과 같은 가상환경을 공유하므로 버전도 맞춰 둔다.
requests==2.32.5
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
joblib==1.5.3

# 사이트 생성. 로컬은 경마 버전 venv 를 공유해 이미 깔려 있었고, 그래서
# 여기 빠진 것이 첫 자동 배포에서야 드러났다(ModuleNotFoundError: yaml).
# 실행에 필요한 것은 전부 여기 적는다 — 로컬에 있다는 것은 근거가 아니다.
Jinja2==3.1.6
PyYAML==6.0.3
